"""SpendWise — a full, self-contained Python expense tracker.

A Flask + stdlib-sqlite3 application with the same smart merchant-resolution
engine, confidence scoring, learning, SMS parsing, fraud detection and analytics
as the FastAPI service — but with **pure-Python dependencies only**, so it can
be embedded inside the Android APK via Chaquopy and run fully offline.
"""

__version__ = "1.0.0"
